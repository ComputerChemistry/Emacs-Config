;;(load "~/.emacs.d/init.el")
;;(load (expand-file-name "init.el" user-emacs-directory))
(setq package-enable-at-startup nil)

;; Asegurarse de que el directorio de elpaca esté en el load-path
;;(setq elpaca-dir (expand-file-name "elpaca" user-emacs-directory))
;;(add-to-list 'load-path elpaca-dir)

;; Cargar el archivo elpaca.el
;;(load (expand-file-name "elpaca.el" elpaca-dir))

;; Inicializar elpaca
;;(elpaca-init)
